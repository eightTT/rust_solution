rust exercices \

/written in rust\
/execute in command-line\
/based in Command-line Rust book by KY Clark\



1. hello app: \
    print out hello world
2. echor (echo rust) app:\
    echo to command-line message 
3. catr (cat rust) app:\
    concatenate all files \
    output the files's contents in order, with or without line numbers
